// Purpose: user can enter their definition of creativity and see
// how it merges with existing definitions of creativity within
// a tree structure.


currentSiblings = 
for (int i = 0; i < currentSiblings.length; i++) {
  if (inputWord != currentSiblings[i]) {
    currentSiblings[currentSiblings.length] = inputWord;
  } else if (inputWord == currentSiblings[i]) {
    // currentSiblings now becomes the children set of the 
    // input word.
    
    // if no childrenSet exists then create a new one.
    
    
void keyPressed() {
  // If the return key is pressed, save the String and clear it
  if (key == '\n' ) {
    saved = typing;
    // A String can be cleared by setting it equal to ""
    typing = ""; 
  } else {
    // Otherwise, concatenate the String
    // Each character typed by the user is added to the end of the String variable.
    typing = typing + key; 
  }
}   

class Unit {
  Unit[] children; String self;
  Unit (String self_in){
    self = self_in;
  }
  
  void addChild(String child_self){
    Unit
 

THis is different! 
